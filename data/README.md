# Data

Do not download raw datasets into this folder before reading and recording the
source documentation in `notes/source-verification.md`.

- `raw/` is for immutable downloaded data when licensing permits local storage.
- `derived/` is for reproducible outputs generated from `raw/` or public APIs.

Prefer scripts that recreate data products from documented sources.
